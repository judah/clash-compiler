[ { "BlackBox" :
    { "name" : "Clash.Signal.Internal.toEnabledClock"
    , "kind" : "Declaration"
    , "type" :
"toEnabledClock
  :: Clock tag -- ARG[0]
  -> Signal tag Bool -- ARG[1]
  -> Clock tag"
    , "template" :
"// toEnabledClock begin~IF ~ISENABLED[0] ~THEN
assign ~RESULT = {~ARG[0][1],~ARG[0][0] & ~ARG[1]};~ELSE
assign ~RESULT = {~ARG[0],~ARG[1]};~FI
// toEnabledClock end"
    }
  }
, { "BlackBox" :
    { "name"      : "Clash.Signal.Internal.unsafeFromReset"
    , "kind" : "Expression"
    , "type" :
"unsafeFromReset :: Reset tag -> Signal tag Bool"
    , "template" : "~ARG[0]"
    }
  }
, { "BlackBox" :
    { "name"      : "Clash.Signal.Internal.unsafeToReset"
    , "kind" : "Expression"
    , "type" :
"unsafeToReset :: KnownDomain tag dom => Signal tag Bool -> Reset tag"
    , "template" : "~ARG[1]"
    }
  }
]

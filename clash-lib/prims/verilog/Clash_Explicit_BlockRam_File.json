[ { "BlackBox" :
    { "name" : "Clash.Explicit.BlockRam.File.blockRamFile#"
    , "kind" : "Declaration"
    , "type" :
"blockRamFile#
  :: (KnownNat m, HasCallStack)-- (ARG[0],ARG[1])
  => Clock tag enabled           -- clk,  ARG[2]
  -> SNat n                    -- sz,   ARG[3]
  -> FilePath                  -- file, ARG[4]
  -> Signal tag Int            -- rd,   ARG[5]
  -> Signal tag Bool           -- wren, ARG[6]
  -> Signal tag Int            -- wr,   ARG[7]
  -> Signal tag (BitVector m)  -- din,  ARG[8]
  -> Signal tag (BitVector m)"
    , "outputReg" : true
    , "template" :
"// blockRamFile begin
reg ~TYPO ~GENSYM[RAM][0] [0:~LIT[3]-1];

initial begin
  $readmemb(~FILE[~LIT[4]],~SYM[0]);
end
~IF ~ISENABLED[2] ~THEN
always @(posedge ~ARG[2][1]) begin : ~GENSYM[~COMPNAME_blockRamFile][2]~IF ~VIVADO ~THEN
  if (~ARG[2][0]) begin
    if (~ARG[6]) begin
      ~SYM[0][~ARG[7]] <= ~ARG[8];
    end
    ~RESULT <= ~SYM[0][~ARG[5]];
  end~ELSE
  if (~ARG[6] & ~ARG[2][0]) begin
    ~SYM[0][~ARG[7]] <= ~ARG[8];
  end
  if (~ARG[2][0]) begin
    ~RESULT <= ~SYM[0][~ARG[5]];
  end~FI
end~ELSE
always @(posedge ~ARG[2]) begin : ~SYM[2]
  if (~ARG[6]) begin
    ~SYM[0][~ARG[7]] <= ~ARG[8];
  end
  ~RESULT <= ~SYM[0][~ARG[5]];
end~FI
// blockRamFile end"
    }
  }
]

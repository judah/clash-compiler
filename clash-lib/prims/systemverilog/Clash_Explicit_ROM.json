[ { "BlackBox" :
    { "name" : "Clash.Explicit.ROM.rom#"
    , "kind" : "Declaration"
    , "type" :
"rom# :: KnownNat n      -- ARG[0]
      => Clock tag enabled -- clk,  ARG[1]
      -> Vec n a         -- init, ARG[2]
      -> Signal tag Int  -- rd,   ARG[3]
      -> Signal tag a"
    , "template" :
"// rom begin
~SIGD[~GENSYM[ROM][0]][2];
assign ~SYM[0] = ~LIT[2];

logic [~SIZE[~TYPO]-1:0] ~GENSYM[~RESULT_q][1];~IF ~ISENABLED[1] ~THEN
always @(posedge ~ARG[1][1]) begin : ~GENSYM[~COMPNAME_rom][2]
  if (~ARG[1][0]) begin
    ~SYM[1] <= ~SYM[0][~ARG[3]];
  end
end~ELSE
always @(posedge ~ARG[1]) begin : ~SYM[2]
  ~SYM[1] <= ~SYM[0][~ARG[3]];
end~FI

assign ~RESULT = ~FROMBV[~SYM[1]][~TYPO];
// rom end"
    }
  }
]

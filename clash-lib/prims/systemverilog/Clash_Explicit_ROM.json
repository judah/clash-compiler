[ { "BlackBox" :
    { "name" : "Clash.Explicit.ROM.rom#"
    , "kind" : "Declaration"
    , "type" :
"rom# :: KnownNat n      --       ARG[0]
      => Clock tag       -- clk,  ARG[1]
      -> Enable tag      -- en,   ARG[2]
      -> Vec n a         -- init, ARG[3]
      -> Signal tag Int  -- rd,   ARG[4]
      -> Signal tag a"
    , "template" :
"// rom begin
~SIGD[~GENSYM[ROM][0]][3];
assign ~SYM[0] = ~LIT[3];

logic [~SIZE[~TYPO]-1:0] ~GENSYM[~RESULT_q][1];~IF ~ISENABLED[2] ~THEN
always @(posedge ~ARG[1]) begin : ~GENSYM[~COMPNAME_rom][2]
  if (~ARG[2]) begin
    ~SYM[1] <= ~SYM[0][~ARG[4]];
  end
end~ELSE
always @(posedge ~ARG[1]) begin : ~SYM[2]
  ~SYM[1] <= ~SYM[0][~ARG[4]];
end~FI

assign ~RESULT = ~FROMBV[~SYM[1]][~TYPO];
// rom end"
    }
  }
]
